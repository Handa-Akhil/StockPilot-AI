# Docker

Container-related configuration for StockPilot AI (images, Dockerfiles, compose overlays).

Root `docker-compose.yml` is the primary compose entrypoint. Additional container files will live here.
